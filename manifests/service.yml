apiVersion: v1
kind: Service
metadata:
    name: "eshoplearnaks-c29a"
    labels:
        app: "eshoplearnaks-c29a"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "eshoplearnaks-c29a"